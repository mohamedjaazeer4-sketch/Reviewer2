# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.env

# uv: the lockfile IS committed on purpose — reproducibility is the whole point.
# (Run `uv sync` to install the exact pinned versions.)

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/

# OS / editor
.DS_Store
.idea/
.vscode/

# Repro artifacts
eval/results/*.json
!eval/results/.gitkeep
